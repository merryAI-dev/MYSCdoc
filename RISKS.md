# RISKS — 개발 리스크 대장과 운영 정책

ROADMAP.md의 자매 문서. "구현 전에 알아야 터지지 않는 것"과 "코드가 아니라 조직이 정해야 하는 것"을 분리해 기록한다.
근거가 코드에 있으면 파일:라인을 남긴다. 항목을 해소하면 지우지 말고 `~~취소선~~ + 해소 커밋` 표기.

---

## A. 실키(GEMINI_API_KEY)를 켜는 순간 터지는 지뢰

### A-1. 자동저장 × rechunk = 임베딩 비용 폭탄 (심각, Phase 1 선행 필수)

- 근거: `DocumentService.java:102`(rename), `:138`(replaceBlocks) — 두 곳 모두 `DocumentChangedEvent` 발행
  → `DocumentChangedListener`(@Async, AFTER_COMMIT) → `ChunkingService.rechunk` → **문서 전체** `embedAll`.
- UI 자동저장이 3초 디바운스 `PUT blocks`이므로, 타이핑 세션 하나가 수십 번의 전체 문서 임베딩 호출이 된다.
  제목 한 글자 수정도 전체 재임베딩이다.
- `rechunk`는 문서 status를 보지 않는다 — **DRAFT도 임베딩**된다. 그런데 검색 쿼리는
  `ChunkRepository`에서 `status IN ('ACTIVE','STALE')`로 DRAFT를 제외하므로 DRAFT 임베딩은 순수 비용 낭비.
- 완화(키 켜기 전에 반드시): ① rechunk 디바운스/코얼레싱(문서당 마지막 이벤트 후 N분 지연 실행, 최신 1건만)
  ② DRAFT 스킵 + ACTIVE 전이 시 1회 청킹 ③ 블록 내용 해시 비교로 무변경 시 스킵.

### A-2. `embedAll`이 트랜잭션 안 (커넥션 풀 고갈)

- 근거: `ChunkingService.rechunk` — `@Transactional` 안에서 네트워크 호출(`ponytail:` 주석으로 명시됨).
  Hikari 기본 풀 10개 × 임베딩 지연 수 초 = 동시 편집 몇 명이면 API 전체가 멈춘다.
- 완화: 임베딩 계산을 트랜잭션 밖으로 (ROADMAP Phase 1-3).

### A-3. Gemini 응답 형식 (교정/요약 파서 파손)

- Gemini는 JSON을 ```json 코드펜스로 감싸는 습성이 있다. `JsonThreadSummaryPort`는 `{`~`}` substring이라
  견디지만, 프롬프트에 "코드펜스 금지"를 넣고 실키로 M5 교정 → M2 검색 순으로 검증한다 (ROADMAP Phase 1-1).
- `gemini-embedding-2`는 `taskType` 미지정 상태 — 검색 품질이 나쁘면 `RETRIEVAL_QUERY`/`RETRIEVAL_DOCUMENT`
  구분 추가가 1순위 튜닝이다.
- 요금/쿼터: 무료 티어 키면 분당 요청 제한으로 rechunk 폭주 시 429 연쇄. A-1과 결합하면 최악.

---

## B. M7 (Google Meet 수집) 특유 리스크

### B-1. smartNotes API가 v2beta (GA 아님)

- 스키마 변경·에디션 제한·Developer Preview 가입 요구 가능성. **구현 전에 실제 계정으로 v2beta GET을 먼저
  curl로 확인**하고, 403/404면 transcript(v2, GA) 경로만으로 시작한다. 스펙(11 문서)의 이벤트 라우팅은
  두 타입을 동일 처리하므로 smartNote를 빼도 구조가 안 바뀐다.

### B-2. Gemini 메모는 "사람이 회의마다 켜야" 생긴다

- "나 대신 메모 작성"은 기본 꺼짐이고 라이선스(Gemini 포함 Workspace 에디션)가 주최자에게 있어야 한다.
  파이프라인을 다 만들어도 **회의에서 아무도 안 켜면 이벤트 자체가 없다.**
- 정책 필요: 공용 주최 계정의 캘린더/관리 콘솔에서 메모·전사 자동 활성화 설정 (수동 설정 절차에 포함).
  안 되는 에디션이면 전사(transcript)만으로 운영.

### B-3. DWD(도메인 전체 위임)의 권한 폭

- `drive.readonly`를 DWD로 열면 서비스 계정 키 = **위임 대상 사용자의 Drive 전체 읽기 권한**.
  키 JSON 파일 유출이 곧 Drive 유출이다.
- 정책: ① 위임 대상은 공용 계정 1개로 한정 (개인 계정 위임 금지) ② 키 파일은 서버에만, 채팅/커밋/공유 금지
  ③ Admin 콘솔에서 해당 클라이언트 ID에 정확히 2개 scope만 허용 ④ 분기별 키 로테이션.

### B-4. fileGenerated 직후 본문이 불완전할 수 있다

- 커뮤니티 보고: `transcript.v2.fileGenerated` 직후 entries가 미완성인 사례. Docs export도 생성 직후엔
  내용이 채워지는 중일 수 있다.
- 스펙(11 문서)은 "빈 본문 = ack 후 무시"인데, 이러면 **조기 도착 이벤트를 영구 유실**한다.
  구현 시 수정할 것: 빈 본문/짧은 본문(예: <200자)은 **nack**으로 바꾸고 B-5의 상한과 짝 지을 것.
  (스펙 대비 변경이므로 QUESTIONS.md에 기록.)

### B-5. poison message — nack 무한 루프

- LLM이 영원히 실패하는 메시지(초장문 전사, 항상 스키마 위반)는 nack 재전달로 pull을 계속 점유한다.
  Pub/Sub 기본 보존 7일 동안 반복.
- 완화: ① 본문 길이 상한(예: 40만 자) 초과 시 앞부분 truncate 후 진행 ② 동일 conferenceRecord 처리
  실패 횟수를 메모리/로그로 세서 N회(예: 10회) 초과 시 ack + ERROR 로그 ③ 데드레터 토픽은 스펙 밖이지만
  운영 안정화 후 1순위 추가 후보.

### B-6. Docs 구조 함정

- Google Docs "탭" 기능: Gemini 회의록이 탭 구조(요약 탭+전사 탭)로 생성되면 `files.export text/plain`에
  전체 탭이 포함되는지 확인 필요. 누락 시 Docs API `documents.get?includeTabsContent=true` 경로로 전환.
- 원본 Docs가 삭제/권한 변경되면 provenance `sourceUrl` 링크가 깨진다. 수집 시점에 본문을 이미
  블록으로 복사하므로 데이터 유실은 없지만, "출처 확인" UX가 깨짐 — 원본 보존 정책(C-4)과 연결.

### B-7. 구독은 소급이 안 된다

- 구독이 끊긴 동안(만료, 서버 다운) 열린 회의는 이벤트가 다시 오지 않는다 = 유실.
  일일 갱신 잡 + expirationReminder 이중화가 스펙에 있지만, **서버가 며칠 꺼져 있으면 그 기간 회의는 없다.**
  소급 수집(conferenceRecords.list 폴링 백필)은 스펙 밖 — 유실 허용이 현 정책임을 인지하고 시작.

---

## C. 코드가 아니라 조직이 정해야 하는 정책

### C-1. 회의 자동 수집의 동의 문제 (가장 중요)

- Meet이 메모/전사 작동을 참석자에게 표시하긴 하지만, 그 결과물이 **검색 가능한 사내 시스템에 자동 게시**되는
  것은 참석자가 모른다. 외부 참석자(고객/파트너)가 있는 회의라면 더 민감하다.
- 현 설계의 안전장치: 수집물은 **DRAFT**로만 생성되고, 사람이 내용을 확인·수정해 검증해야 ACTIVE(검색 노출)가
  된다. 이 "사람 게이트"를 절대 우회하지 말 것 (자동 ACTIVE 전이 기능 요청이 와도 거절 근거가 이 항목).
- 정해야 할 것: ① 수집 대상 회의 기준(공용 계정 주최 = 수집 동의로 간주한다는 사내 공지) ② 외부 참석자
  포함 회의 처리(수집 제외? 별도 스페이스?) ③ 원본 전사 보존 기간.

### C-2. 배포 경계 — 지금 아키텍처는 사내망 전용

- `X-Member-Id` 헤더 인증은 **사실상 인증이 아니다** (UUID만 알면 누구든 위장 가능). Phase 2 OAuth 전까지
  공인 IP/외부 노출 배포 절대 금지. "일단 사내 서버에 올려보자"가 가장 위험한 순간이다.
- space 멤버십도 없어서 로그인만 하면 모든 문서 열람·수정 가능 — 민감 문서(인사/재무)는 파일럿 기간에
  아예 넣지 않는 것이 정책.

### C-3. 동시편집 last-write-wins (Phase 2 전까지)

- 현 UI는 REST 저장이라 두 명이 같은 문서를 열면 늦게 저장한 쪽이 이긴다(먼저 저장한 내용 소실).
  공동편집 연결 전 파일럿 운영 관행: "한 문서는 한 명이 편집" 안내 + 리비전 이력으로 복구 가능함을 공지.

### C-4. 수집 DRAFT 누적 관리

- Slack/Meet/Tiro가 모두 DRAFT를 만든다. 아무도 검증하지 않은 DRAFT가 쌓이면 목록이 쓰레기장이 된다.
  정책 후보: N일(예: 30일) 지난 미검증 수집 DRAFT 자동 ARCHIVED + owner에게 DM. (StalenessJob 패턴 재사용
  가능 — 구현 전 스펙 합의 필요.)

### C-5. 시크릿 취급 (이미 2회 사고)

- 이 프로젝트 진행 중 API 키가 채팅에 평문으로 2번 노출됐다(즉시 재발급 권고됨). 규칙 명문화:
  ① 키·토큰은 `.env`와 시크릿 매니저에만 — 채팅/커밋/스크린샷 금지 ② 노출 즉시 폐기·재발급
  ③ `GOOGLE_APPLICATION_CREDENTIALS` JSON도 동일 취급 ④ 실키와 테스트 키를 분리 발급.

### C-6. 비용 가시성

- 실키 운영 시작과 동시에: Gemini 사용량 대시보드(AI Studio/GCP 콘솔) 주간 확인 담당 지정,
  월 예산 상한 알림 설정. A-1 완화가 배포 전 조건인 이유이기도 하다.
- 회의 수집 LLM 비용은 "회의 수 × 전사 길이"로 선형 — 공용 계정 1개 한정이 곧 비용 통제 장치.

---

## 우선순위 요약

| 시점 | 반드시 먼저 |
|---|---|
| 실키 켜기 전 | A-1 (rechunk 디바운스 + DRAFT 스킵), A-2 (트랜잭션 밖), C-6 (예산 알림) |
| M7 구현 전 | B-1 (v2beta curl 확인), B-2 (에디션/자동화 설정), C-1 (수집 동의 공지) |
| M7 구현 중 | B-4 (빈 본문 nack로 스펙 수정), B-5 (poison message 상한) |
| 사내 배포 전 | C-2 (사내망 한정 확인), C-3 (편집 관행 공지), C-5 (키 분리) |
