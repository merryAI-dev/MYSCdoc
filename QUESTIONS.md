# QUESTIONS

- RESOLVED (2026-07-08, M12+: 반려돼도 문서는 누적, 트리플만 생략 + 요약 품질 기준 명문화): 보람 지적("문서를 아예 안 만들지는 말고 트리플렛만 안 만들면 되지 않을까? 누적·아카이빙은 되어야 해서") — 검증 에이전트가 반려해도 `DecisionExtractionJob.persist()`가 문서·블록은 그대로 생성/갱신하도록 바꿨다(요약·의사결정·암묵지 섹션 전부 포함, 상단에 "⚠️ 검증 미통과 — 사람이 확인해 주세요" 배너 + 검증 사유를 남김). 다만 `replaceTriples()`는 `verified`일 때만 호출해 지식그래프(BM25 검색·시각화 대상)에는 검증된 트리플만 올라가게 했다 — 문서(누적 아카이브, 낮은 신뢰 기준)와 지식그래프(구조화 지식, 높은 신뢰 기준)의 신뢰 레벨을 분리한 것. 재추출로 이전엔 검증됐던 트리플이 이번엔 반려되면 기존 트리플도 함께 내린다(그래프는 항상 "현재 검증 상태"만 반영). 이어서 보람 확인("요약이 충실해야 아카이브로서 의미가 있다")에 따라 요약 프롬프트에 별도 "## 요약 작성 기준" 섹션을 추가 — 원문 없이 요약만 읽어도 무엇/누가/어떻게 마무리됐는지 파악되게, 발화자 역할 명시, 숫자·기한·에러메시지 같은 검색 가능한 사실은 뭉개지 않기를 명문화했다(JSON 스키마 예시 안에 여러 문장을 넣으면 모델이 그 설명 자체를 출력하는 것으로 오해할 수 있어 스키마는 한 줄로 유지하고 기준은 별도 섹션으로 분리). M9AcceptanceTest의 반려 시나리오를 "문서 안 만듦"에서 "문서는 만들되 트리플 0건"으로 갱신. 로컬 전체 스위트 실행 중 Docker 컨테이너 20개 경합으로 Testcontainers Ryuk 연결 실패(내 로직과 무관한 인프라 이슈, 단독 재실행도 동일 실패) — CI(클린 환경)로 최종 확인.

- RESOLVED (2026-07-08, M12: 실제 슬랙 히스토리 백필 + 2-패스 추출/검증 에이전트, 실채널 실증): 보람 지적("네가 손으로 스레드 골라 curl로 DB에 넣지 말고, 실제 슬랙 연동 기능 + Gemini가 자동으로 하는 루프를 만들어야") — M9는 Socket Mode로 *새 메시지*만 잡았고, 과거에 쌓인 논의를 지식화하려면 시스템이 직접 `conversations.history`+`conversations.replies`를 읽어야 했다. `SlackApiGateway.channelHistory()`(봇/시스템 subtype/빈본문 제외)와 `SlackBackfillService`(옵트인·중복제거는 `SlackArchiveService` 재사용) + `POST /api/slack/channels/{id}/backfill`(백필 직후 자동으로 syncNow 체이닝) + `/knowledge.html` "가져오기" 버튼을 추가했다. 두 번째로, 보람 요청("DriveOps에서 하듯 에이전트끼리 소통하게")에 따라 런타임 2-패스 Generator–Evaluator를 넣었다: 추출 에이전트(`JsonDecisionExtractPort`) → 검증 에이전트(`JsonDecisionVerifyPort`, 별도 Gemini 호출)가 원문 스레드와 대조해 환각·오탐이면 반려. 검증은 fail-open(호출 실패 시 승인)이라 검증 삐끗이 정상 추출의 데이터 손실로 이어지지 않는다. 세 번째로, 게이트를 `hasDecision`에서 `worthRecording`으로 완화 — 실제 인사이트 채널 스레드("Fable 5 써보세요, 7일까지 프로모션")를 테스트하다 "추천/정보공유는 결정이 아니다"를 프롬프트에 명확히 하는 한편, "해결됨+원인"처럼 **결정 없이 지식만 있는 스레드도 기록**하도록 완화했다(todoc의 본질이 의사결정뿐 아니라 조직 지식 기록이므로). 실제 `#axr-qa` 채널(옵트인 후 백필)로 전체 루프를 돌려 **검증 에이전트가 실제로 오탐을 반려하는 것을 실증**했다 — 추출 에이전트가 "decision"으로 뽑은 스레드를 검증 에이전트가 "확정된 결정이 아니라 제안·논의에 가깝고, risk 트리플은 원문 근거가 없다"는 구체적 이유로 반려, 문서 미생성. 이 과정에서 실제 버그를 하나 발견했다: `SlackArchiveService.archive()`가 `created_at`을 (Slack 원 발화 시각이 아니라) **아카이브에 넣은 시각**으로 찍어서, 백필로 가져온 과거 대화가 곧바로 "조용해짐" 판정을 못 받고 `quietMinutes`만큼 다시 기다려야 한다 — 백필 직후 바로 판별하고 싶다면 후속 과제로 `channelHistory`가 원본 타임스탬프를 넘기고 `archive()`가 그걸 `created_at`으로 쓰도록 바꿔야 한다(현재는 미수정, ROADMAP 후보). M9AcceptanceTest에 결정없는지식문서화·검증반려·설정API 튜닝 시나리오를 추가해 로컬 통과(9개 시나리오).

- RESOLVED (2026-07-06, M7 구현): `11-ingest-meet.md`의 원문 ack/nack 표는 "빈 본문 → ack"와 Acceptance 4 "예외 없음"을 요구하지만, `RISKS.md B-4`와 이번 작업 지시에 따라 조기 `fileGenerated` 이벤트에서 Google Docs 본문이 아직 비어 있거나 너무 짧을 수 있는 문제를 우선했다. 구현은 공백 또는 200자 미만 본문을 `MeetRetryableException`으로 처리해 Pub/Sub `nack`/재전달을 유도한다. 이 정책 편차는 `M7AcceptanceTest.blankOrShortBodyIsNackedForPubSubRetry`로 고정했다. 같은 흐름에서 poison message 방지를 위해 conferenceRecord별 10회 실패 시 예외를 삼켜 ack되도록 하고, LLM 입력 본문은 400,000자로 truncate한다.
- RESOLVED (2026-07-06, M7 구현): `11-ingest-meet.md`는 Workspace Events 구독 생성에 `notificationEndpoint.pubsubTopic`이 필요하다고 쓰지만 `.env.example`에는 Pub/Sub subscription(`MEET_PUBSUB_SUBSCRIPTION`)만 둔다. 새 환경변수를 추가하지 않기 위해 `MeetSubscriptionJob`은 허용된 `google-cloud-pubsub`의 `SubscriptionAdminClient`로 Cloud Pub/Sub subscription의 topic을 조회한 뒤 Workspace Events `subscriptions.create`에 사용한다. 즉 수동 GCP 설정은 spec 그대로 `projects/{P}/subscriptions/{S}`만 주입하면 되고, 별도 topic env는 만들지 않았다.
- RESOLVED (보람 승인 2026-07-04, AXR팀 협의 완료): Switched LLM/embedding provider from OpenAI + Anthropic to Gemini, replacing `spring.ai.model.embedding`/`spring.ai.model.chat` with a single `GEMINI_API_KEY`. First attempt used the official `spring-ai-starter-model-google-genai` (Spring AI 1.1.8; only Gemini starter available in Spring AI, since Vertex AI Gemini requires GCP project/service-account auth and doesn't fit a plain API key). That starter's `com.google.genai:google-genai` SDK pulls a heavy transitive tree (gRPC, protobuf, OpenCensus, Guava 33, Kotlin stdlib, OkHttp, Apache HttpClient) that made Spring's autoconfiguration condition-evaluation phase alone take ~54s, ballooning `MydocApplication` startup from ~10s to ~136s and one Testcontainers test class from ~40s to 9m25s — confirmed with a real `bootRun` boot-log timing breakdown, not guessed. Reverted to a small hand-rolled REST client instead: `GoogleGenAiChatClient`/`GoogleGenAiEmbeddingAdapter` (`src/main/java/com/mysc/mydoc/ai/`) call the Gemini Developer API directly (`generateContent`, `embedContent`/`batchEmbedContents`) via Spring's own `RestClient`, gated by `@ConditionalOnExpression("'${mydoc.gemini.api-key:}' != ''")` — the same pattern `SlackSocketModeRunner` already used for optional Slack tokens. No new dependency was added; `spring-ai-bom`/the Gemini starter were removed entirely, and `AiModelEnvironmentPostProcessor` (no longer needed — nothing to exclude) was deleted along with its `META-INF/spring.factories` registration. Startup time is back to ~10s (confirmed: the gap between "No active profile" and "Bootstrapping Spring Data JPA repositories" dropped from 54s to 2.4s) and a full `./gradlew clean build` (M0-M6) passed in 3m19s. Embedding output dimension is pinned to 1536 via `mydoc.gemini.embedding-dimensions` (Gemini's `embedContentConfig.outputDimensionality`) to match the existing `vector(1536)` column in V2__search.sql without a new migration. Chat model: `gemini-2.5-flash`; embedding model: `gemini-embedding-2` — both configurable via one `application.yml` line if a different tier is wanted later. Real-key acceptance (does the Gemini output quality/JSON-shape match what `CorrectionService`/`JsonThreadSummaryPort` expect) is unverified — same category as the earlier OpenAI/Anthropic real-key P0s.

- RESOLVED: Independent concurrency/security audit (2026-07-04) found and fixed 4 real defects with no API keys required: (1) `ChunkingService.rechunk` had no document lock, so concurrent rechunks for one doc could interleave delete/embed/save and leave stale search chunks — now serialized via `DocumentService.getLocked` (PESSIMISTIC_WRITE). (2) `Document` had no `@Version`, so the Monday-Friday 9am `StalenessJob` (no lock) could silently overwrite a document a user had just verified — added `@Version` (V5 migration) and reworked the job to run per-document under a pessimistic lock with a staleness re-check, skipping on optimistic-lock conflict instead of failing the whole batch; conflicts now map to 409 for API callers. (3) `HeaderAuthFilter`'s internal-token comparison used `String.equals` (timing side-channel) — switched to `MessageDigest.isEqual`. (4) editing-plane `flushSnapshot`'s null-editor guard checked the timer map, which a retry had already cleared on its first attempt, silently dropping the snapshot — retries now force the pending flag. A candidate fix for Slack duplicate-ingest (claim the thread via the ingest log before the expensive summarize call) was tried and reverted: it requires a placeholder document to satisfy the `slack_ingest_log.document_id` FK, and catching `DataIntegrityViolationException` to distinguish "lost the race" from "genuine DB error" is unsafe once Postgres marks the transaction aborted after a constraint violation. The underlying race (wasted LLM call + duplicate Slack reply on truly concurrent reactions on the same thread) remains and is low severity — the unique constraint already prevents a duplicate persisted document. Full `./gradlew clean build` (Testcontainers, M0-M6) and a live `docker compose up -d && bootRun` curl smoke test (space/document/blocks/verify/internal-token paths) both passed after these changes. Three IDOR-shaped findings from the same audit (any member can rename/edit any document; MCP `create_draft`/`get_document` cross spaces) were checked against `03-api-spec.md`'s permission table and rejected as by-design: only verify/archive/owner-change are restricted to owner/ADMIN, and there is no space-membership concept in the spec at all.
- RESOLVED: `01-tech-stack.md:10` says `hibernate-vector` uses the Boot BOM version, but Spring Boot 3.3.5 dependency management does not provide a version for that artifact directly. The build now derives `hibernate-vector` from Boot's managed `org.hibernate.orm:hibernate-core` version so it stays on the Boot-managed Hibernate line without a hard-coded version.
- RESOLVED: `01-tech-stack.md:8` lists `spring-boot-starter-security`, while the M0 dependency block omits it. I added Spring Security with the existing `X-Member-Id` filter populating `SecurityContext` and a minimal `SecurityFilterChain` that preserves the MVP header-auth contract.
- RESOLVED (보람 승인 2026-07-04): `01-tech-stack.md:15` fixes Testcontainers to 1.19.x, but the latest 1.19.x is 1.19.8 and it pulls docker-java 3.3.6, which fails against local Docker Engine 29.1.3 / Docker API 1.52 with `Could not find a valid Docker environment` and Docker `/info` HTTP 400. Testcontainers 2.0.5 (docker-java 3.7.1) stays, along with the corresponding `testcontainers-postgresql` / `testcontainers-junit-jupiter` module names.
- RESOLVED: `02-domain-model.md` includes `Chunk` and `SlackIngestLog` entities/repositories, while `04-database.md` creates their tables only in V2 and V4. I added them at their migration milestones so Hibernate validation stays green through the milestone sequence.
- RESOLVED (보람 승인 2026-07-04): `01-tech-stack.md:41` names `org.springframework.ai:spring-ai-openai-spring-boot-starter`, but Maven Central returns 404 for Spring AI 1.0.9 at that coordinate and 200 for `org.springframework.ai:spring-ai-starter-model-openai`. The published 1.0.x artifact stays.
- RESOLVED: Adding Spring AI starters makes model autoconfiguration sensitive to missing API keys. The app now enables OpenAI embeddings when `OPENAI_API_KEY` is set and Anthropic chat when `ANTHROPIC_API_KEY` is set; otherwise those model types stay disabled. No extra `SPRING_AI_MODEL_*` env vars are required.
- RESOLVED (2026-07-06, 실키 Gemini로 기계 검증): M2 real semantic search now runs end-to-end with the real `GEMINI_API_KEY` (provider was switched from OpenAI to Gemini): a block save triggered the debounced rechunk, `gemini-embedding-2` embedded the chunk (verified `embedding IS NOT NULL` in DB), and `GET /api/search?q=연차 어떻게 써` ranked the 휴가-절차 document first. Two latent defects were found and fixed during this: (1) Spring's RestClient picked up OkHttp (transitive via the Slack SDK) whose 10s default read timeout killed every real Gemini call as a 500 — both Gemini clients now set explicit connect 10s / read 120s timeouts, and chat disables thinking (`thinkingBudget: 0`) since correction/summary are structured-output tasks (latency dropped to ~4s); (2) the web UI called `/api/search?query=` while the spec/controller use `q`, silently returning empty results — fixed. Remaining human judgment: ranking *quality* needs evaluation with a real corpus (currently 2 docs; KNN returns neighbors for unrelated queries too — a similarity threshold is a tuning candidate).
- RESOLVED: `05-editing-plane.md` requires Java to call the editing-plane kick endpoint but does not define a Java-side env var. Java now uses the spec's local editing-plane port, `http://localhost:8081`, as the default Spring property without adding another `.env.example` entry.
- RESOLVED: `06-ingest-slack.md` requires replying with a document URL, and `08-mcp-server.md` defines `BASE_URL` as `MYDOC_BASE_URL`. Slack replies, stale DMs, and MCP URLs all use `MYDOC_BASE_URL` with local default `http://localhost:8080`.
- RESOLVED (보람 승인 2026-07-04): `02-domain-model.md` says `replaceBlocks` transitions every DRAFT document to ACTIVE, but `10-milestones.md:M4 Acceptance 1` requires Slack-ingested documents to remain DRAFT and `08-mcp-server.md:create_draft` requires AI drafts to remain DRAFT. `SLACK_INGEST` and `AI_SUGGESTION` stay as DRAFT-preserving causes per the milestone acceptances.
- RESOLVED: `06-ingest-slack.md` says M4 includes summary LLM integration, while `01-tech-stack.md` adds the Anthropic starter at M5. The existing Anthropic `ChatModel` client now implements both correction and Slack thread summary calls once chat is enabled.
- RESOLVED (2026-07-07, M4 Socket Mode 실키 검증): M4 real Slack ingestion. A real MYSC-workspace bot token (bot username `innerplatformalerts`; the Slack *app* itself is actually named **MerryLetter**, App ID `A0AMSS0SPU2` — the bot username and the app name are different things, which caused confusion trying to find "innerplatformalerts" in the api.slack.com/apps list) is wired into mydoc/.env and covers all required scopes (`reactions:read`, `reactions:write`, `channels:history`, `groups:history`, `chat:write`, `users:read`, plus a few unrelated scopes from MerryLetter's other uses). An `xapp-` App-Level Token (`connections:write` scope) was generated on the MerryLetter app's Basic Information page, and Socket Mode + the `reaction_added` bot event were enabled there. Booting with both tokens present exposed a second real defect — this time a packaging gap, not a code bug: `com.slack.api:bolt-socket-mode:1.49.0`'s `Slack.socketMode()` requires a JSR-356 (`javax.websocket`) client implementation on the runtime classpath, and neither `bolt-socket-mode` nor `slack-api-client` bring one transitively. The app crashed at startup with `NoClassDefFoundError: javax/websocket/CloseReason$CloseCode` the first time Socket Mode was actually exercised (this never surfaced before because `SLACK_APP_TOKEN` had always been empty, so the bean's `@ConditionalOnExpression` kept it disabled). Fixed by adding `runtimeOnly 'org.glassfish.tyrus.bundles:tyrus-standalone-client:1.20'` to `build.gradle`. After the fix, `bootJar` succeeds and the boot log shows `SlackSocketModeRunner: Slack Socket Mode started`. An earlier bean-wiring bug (`SlackApiGateway` had two constructors and Spring couldn't pick one) had already been fixed separately. A live end-to-end ingest test (bot posts to a real MYSC channel, reacts to its own message, confirms a DRAFT document is created) was intentionally skipped per 보람's call — posting/reacting in a real company channel needs a deliberate test-channel decision, and the actual blocker (Socket Mode connectivity) is now confirmed live.
- RESOLVED (보람 승인 2026-07-04): `01-tech-stack.md:43` names `org.springframework.ai:spring-ai-anthropic-spring-boot-starter`, but Maven Central returns 404 for Spring AI 1.0.9 at that coordinate and 200 for `org.springframework.ai:spring-ai-starter-model-anthropic`. The published 1.0.x artifact stays.
- RESOLVED (2026-07-06, 실키 검증 통과): M5 real correction now verified with the real `GEMINI_API_KEY` (provider switched from Anthropic to Gemini): `POST /api/documents/{id}/corrections` returned score 85 with 2 schema-valid findings in ~4.4s. Gemini did wrap the JSON in a ```json code fence as predicted in RISKS A-3 — the existing `indexOf('{')`/`lastIndexOf('}')` parser strips it, no prompt change needed. The failure the user reported ('key not applied') was actually the OkHttp 10s read-timeout defect described in the M2 entry above, surfacing as an unexplained 500; RestClientException is now mapped to a 502 ProblemDetail so upstream failures are distinguishable from bugs.
- RESOLVED: `10-milestones.md:13` requires `docker compose up -d && ./gradlew bootRun`. Local port 5432 was held by `sim-db-1`; I temporarily stopped it, ran the M0 curl checks, then restored it.
- RESOLVED: `09-coding-guidelines.md:39` requires at least one `M0: ...` commit. I initialized git in `/Users/boram/mydoc` after M0 verification.
- RESOLVED (2026-07-08, M11++: 배포 전 코드 보강 + Fly.io 배포 경로): DEPLOY-PLAN.md의 "높음" 게이트를 코드로 해소했다. (1) Hikari 풀 명시 — `maximum-pool-size` 기본 5(`DB_POOL_SIZE`로 조정), `max-lifetime` 28분(Cloud SQL/MPG가 유휴 커넥션을 끊기 전에 앱이 먼저 재생성). (2) Flyway 계정 분리 지원 — `FLYWAY_DB_USERNAME/PASSWORD` env가 있으면 DDL 전용 계정으로 마이그레이션, 없으면 기존 datasource 계정(로컬 무변화). (3) Gemini 재시도 — `GeminiRetry` 헬퍼가 429/5xx만 지수 백오프(0.5→1→2s)+jitter로 최대 3회, 타임아웃은 재시도 안 함(120s read timeout 재시도는 지연만 배가). chat/embedding 두 클라이언트에 적용. (4) 보안 헤더 — `SecurityConfig`에 CSP(`script-src 'self' 'unsafe-inline' https://esm.sh`, `object-src 'none'`, `frame-ancestors 'none'`)와 X-Frame-Options DENY. 인라인 스크립트는 현 UI 구조상 허용하되 외부 출처 주입·프레이밍·MIME 스니핑 차단. (5) 프론트 에러 피드백 3곳 — 그래프 로드 실패를 "트리플 없음"과 구분, 검색 실패를 "결과 없음"으로 위장하던 것 수정, loadNav 실패 시 부팅이 조용히 죽지 않게. 배포 경로는 GCP 대신 **Fly.io**로 결정(core 상시가동이라 Cloud Run scale-to-zero 못 쓰는데 그럴거면 Cloud SQL 비용이 아까움 — 월 $40~75 vs Fly $5~15). DB는 self-host `pgvector/pgvector` 컨테이너(`deploy/postgres/fly.toml`) — MPG는 앱 계정이 슈퍼유저가 아니라 CREATE EXTENSION 불가하고 이 flyctl 버전엔 --pgvector 플래그도 없어서, postgres 슈퍼유저인 self-host 이미지가 V1의 CREATE EXTENSION vector를 바로 통과시킨다. Fly 단일 머신(min=max=1)이라 DEPLOY-PLAN B2/B3/B4(잡·Socket 중복, 커넥션풀)가 자동 해소. 남은 배포 게이트는 B1(공개 URL 차단 → Cloudflare Access로 결정)·B7(esm.sh 도달 확인). 배포 절차는 DEPLOY-FLY.md. 보강 검증: 로컬 과부하 전체 빌드에서 M2 2건이 자원 경합으로 flaky 실패했으나 M2 단독 재실행 통과(BUILD SUCCESSFUL 7m) + M9(7)·M7(7) 통과로 확인, CI가 클린 환경에서 전체 스위트 재검증.

- RESOLVED (2026-07-08, M11+: 수동 싱크 + 배포 3자 검토 + XSS 수정): (1) 수동 싱크 — 오후 6시 cron을 기다리지 않고 지금 즉시 추출을 돌리는 `POST /api/knowledge/sync`와 `/knowledge.html`의 "지금 동기화" 버튼. `DecisionExtractionJob.run()`을 `syncNow()`로 리팩터해 처리한 스레드 수(examined)와 문서화 건수(documented)를 리턴하고, `AtomicBoolean` 가드로 스케줄·수동이 겹치거나 버튼 연타해도 한 번에 하나만 돌게 했다(인스턴스 내 한정 — 다중 인스턴스면 DB 락 필요하나 core는 배포상 단일 인스턴스로 고정). (2) 실배포 전 DBA·백엔드·프론트 3개 서브에이전트가 각 관점에서 리스크를 도출 → `DEPLOY-PLAN.md`로 종합. 세 관점이 독립적으로 같은 결론 2개에 도달: **core는 min=max=1 단일 인스턴스 고정**(@Scheduled 잡·Slack Socket·Pub/Sub pull이 다중 인스턴스에서 중복 실행/중복 수신/CPU 정지 — DEPLOY.md 목표구성표의 "무상태 오토스케일 허용"이 코드와 상충해 정정함) + **공개 URL 금지**(X-Member-Id는 UUID만 알면 위장 가능, 인증 아님 — `--no-allow-unauthenticated`/internal ingress 필수, RISKS C-2). 그 외 배포 차단급으로 CREATE EXTENSION 권한 순서, 커넥션 풀 폭발(오토스케일 시 첫날 장애), TipTap esm.sh CDN 의존을 식별. (3) 프론트 에이전트가 이번에 내가 만든 `knowledge.html` detail 패널의 XSS(트리플 값을 escape 없이 innerHTML)를 배포 차단 이슈로 지목 → innerHTML을 textContent + createElement로 교체해 즉시 수정(트리플은 Slack/LLM 추출물이라 신뢰 불가). 나머지 배포 게이트(최소권한 2계정, ivfflat 재학습, Gemini 재시도, CSP 헤더, .dockerignore .env 등)는 DEPLOY-PLAN.md에 우선순위별로 체크리스트화했다. 수동 싱크는 M9AcceptanceTest에 REST 검증 추가해 로컬 통과.

- RESOLVED (2026-07-08, M11: 아카이빙 채널 옵트인 + 잡 미세조정 API + 매일 18시 스케줄): 보람 요청("매일 오후 6시 실행, 보이는 슬랙 중 아카이빙할 곳을 고르는 UI, API로 미세조정"). 세 가지를 넣었다. (1) 채널 옵트인 — `slack_channel_config` 테이블(V10)에 켠 채널만 `SlackArchiveService`가 수집한다. 이전(M9)엔 봇이 초대된 모든 채널을 수집했는데, 이제 초대는 필요조건일 뿐이고 `/knowledge.html` 설정에서 명시적으로 켜야 수집된다(RISKS C-1 동의 원칙을 코드로 강제 — 회사 채널을 무단 아카이빙하지 않음). `SlackApiGateway.memberChannels()`가 `conversations.list`로 봇이 멤버인 채널을 커서 페이지네이션으로 모아 주고, UI가 저장된 on/off와 머지해 체크박스로 보여준다. `SlackGateway.memberChannels()`는 default 메서드로 둬서 기존 테스트 fake들이 안 깨지게 했다. (2) 미세조정 — `knowledge_setting` 단일행 테이블(V10, quiet_minutes/min_messages)을 `GET/PUT /api/knowledge/settings`로 읽고 쓴다. 잡이 매 실행 시 이 값을 읽어 `findQuietThreads(minMessages, now-quietMinutes)`에 넘긴다. 상수 하드코딩이던 QUIET_PERIOD/MIN_MESSAGES를 DB 설정으로 옮긴 것. (3) 스케줄 — `@Scheduled(fixedDelay 5분)`을 `@Scheduled(cron "0 0 18 * * *", zone Asia/Seoul)`로 바꿔 매일 퇴근 무렵 하루치 논의를 한 번에 문서화한다. cron은 `SLACK_DECISION_CRON`으로 오버라이드 가능하고, 테스트는 `"-"`(disabled cron)로 스케줄러를 꺼 job.run()을 직접 호출한다. 채널 토글·설정 API·미세조정 반영을 M9AcceptanceTest에 추가해 로컬 통과. 중요: **BM25 검색과 force-directed 그래프는 AI가 아니라 순수 수식 계산**이다(서버 BM25 점수, 브라우저 물리 시뮬레이션, 토큰 비용 0) — Gemini는 스레드에서 의사결정/암묵지를 추출하는 단계에서만 쓰인다. 채널 옵트인 UI를 붙이면서 이 경계를 사용자에게 명확히 안내함.

- RESOLVED (2026-07-08, M10: 지식 온톨로지 저장 + BM25 검색 + 그래프 시각화): M9가 추출한 의사결정·암묵지를 문서 블록 텍스트로만 남기지 않고 `knowledge_triple` 테이블(V9)에 (subject, predicate, object) 트리플로 영속화한다 — 의사결정도 (owner|팀, "결정했다", 결정내용) 트리플로 저장해 그래프에서 함께 보인다. 재추출 시 해당 문서의 트리플을 통째로 교체해 문서 블록과 항상 일치시킨다. 검색은 LLM이나 외부 라이브러리 없이 **Okapi BM25 수식을 그대로 구현**했다(`Bm25.java`에 score/IDF 수식 주석 포함, k₁=1.2, b=0.75, Lucene 변형 IDF). 한국어는 형태소 분석기 없이 CJK 문자 bigram 토큰화(Elasticsearch CJK analyzer와 같은 접근)로 처리하고, 라틴/숫자는 단어 단위. 코퍼스 통계(df, avgdl)는 질의 시점에 전체 트리플로 구성 — 수천 건까지는 충분하고 그 이상이면 역색인 전환이 과제. API: `GET /api/knowledge/triples?q=`(BM25 랭킹, q 없으면 최신순), `GET /api/knowledge/graph?q=`(nodes/edges). 시각화는 `/knowledge.html` — 외부 CDN 없이 캔버스에 힘-방향(force-directed) 레이아웃을 직접 구현(쿨롱 반발력 F=K_R/d², 훅 스프링 F=K_S(d−L), 중심력, 감쇠 오일러 적분), kind별 색상, 간선 hover 시 statement 패널, BM25 검색창 연동, index.html 사이드바에 "🕸 지식그래프" 링크 추가. `/d/{id}` 문서 링크는 서버 라우트가 없어(슬랙 답장용 URL로만 존재하는 기존 갭) 그래프에서 문서로 점프하는 링크는 이번엔 넣지 않았다. M9AcceptanceTest에 트리플 생성/교체 검증과 REST로 BM25 랭킹·그래프 shape 검증을 추가해 로컬 Testcontainers로 통과 확인.

- RESOLVED (2026-07-08, M9: Slack 상시 아카이브 + 의사결정/암묵지 추출): ROADMAP.md Phase 2가 그리던 "리액션 없이도 문서가 갱신되는 구조"의 첫 조각. 기존 M4는 사람이 🔖 이모지를 달아야만 스레드를 수집했는데(리액션 의존), M9은 봇이 초대된 채널의 모든 message 이벤트를 상시 아카이브(`slack_archive_message`, append-only, (channel_id, ts) unique로 재전달 dedup)하고, 30분 이상 조용해진 3개 이상 메시지 스레드를 5분 주기 `DecisionExtractionJob`이 Gemini로 판별한다. 의사결정이 없으면(잡담/정보공유) 문서를 만들지 않고 `slack_decision_log`에 기록만 남겨 재판별을 막는다(lastTs 비교로 LLM 재호출 skip). 의사결정이 있으면 DRAFT 문서를 만들고, 스레드가 이어지면 그 문서가 아직 DRAFT일 때만 갱신한다(사람이 검증해 ACTIVE로 만든 문서는 덮어쓰지 않음 — M4의 "SLACK_INGEST는 DRAFT 유지" 정책과 동일 원칙). 봇이 초대되지 않은 채널·DM(im/mpim)은 애초에 이벤트가 안 오고, 코드에서도 channelType 화이트리스트로 한 번 더 걸러 "채널 초대 = 수집 동의" 원칙을 지킨다. 두 번째로, 추출 자체를 요약뿐 아니라 **의사결정 포인트**(decision/rationale/alternatives/owner/condition)와 **조직의 암묵지**(kind별 statement + subject-predicate-object 트리플)까지 함께 뽑도록 프롬프트를 다시 설계했다 — PTCF(Persona-Task-Context-Format) 구조, 대명사를 실제 지칭 대상으로 바꾸는 coreference 지시, 스레드 전체에서 같은 개체를 하나의 이름으로 통일하는 entity-consistency 지시, 1-shot 예시를 넣어 Gemini 2.5 Flash가 코드펜스 없이 안정적인 JSON을 내도록 했다(`JsonDecisionExtractPort`). 트리플은 아직 별도 그래프 저장소 없이 문서 블록 안에 "주어 — 서술어 — 목적어" 텍스트로 보존한다(온톨로지 저장소는 향후 과제로 ROADMAP에 남김). `M9AcceptanceTest`(fake ThreadSummaryClient)는 아카이브 dedup, 의사결정→DRAFT+갱신, 비의사결정→기록만, 짧은/아직 활성인 스레드는 LLM 호출 자체를 안 하는 것까지 로컬에서 통과 확인(Testcontainers, 두 차례 완주). 새 env 없음 — Slack 앱 대시보드에서 `message.channels`/`message.groups` 봇 이벤트 구독만 추가하면 된다.

- RESOLVED (2026-07-07, M8: Tiro 회의록 가져오기, 실키 검증 통과): ROADMAP.md의 Tiro 연동 설계는 웹훅 push 방식을 상정했으나(Tiro 대시보드 등록·시그니처 검증·비동기 재시도 필요), 실사용 요구가 "사용자가 명령어를 입력하지 않고 버튼으로" 였기 때문에 대신 **UI 버튼 트리거 pull 방식**으로 구현했다 — 사이드바 "⇩ Tiro에서 가져오기" → 키워드 검색 패널 → 선택 시 즉시 가져오기, 웹훅 인프라 불필요. `com.mysc.mydoc.ingest.tiro` 패키지(TiroPort/TiroClient/TiroIngestService), `TiroController`, `V7__tiro_ingest.sql`(dedup), 이미 존재하던 미사용 `ChangeCause.IMPORT`/`SourceType.IMPORT`를 DRAFT 유지 cause 목록에 추가해 재사용. 실제 Tiro API 키(워크스페이스: MYSC)로 전체 흐름 검증: 목록/키워드검색 → 가져오기(Gemini가 전사를 3섹션 JSON으로 구조화, DRAFT 생성) → 재가져오기 시 중복 없이 기존 문서 반환(`tiro_ingest_log` unique) → 존재하지 않는 노트 요청 시 502 ProblemDetail. 실키 검증 중 실제 API 문서와 다른 점 2개 발견: (1) `GET .../notes`의 목록 크기 파라미터는 `limit`이 아니라 `size`(문서 오독으로 인한 내 버그, default 100이라 조용히 무시됐음 — 고침) (2) `keyword`는 엄격한 부분일치가 아니라 "느슨한 관련도순 재정렬"이 Tiro의 설계된 동작(문서에 명시돼 있었음, 버그 아님). RISKS.md C-1(자동 수집 동의 문제)은 이 경로엔 약하게 적용된다 — 사용자가 특정 회의를 명시적으로 선택해 가져오는 행위라 Slack/Meet의 수동적 자동 수집과 다르지만, 동일한 DRAFT→검증 게이트는 그대로 유지했다. `M8AcceptanceTest`(fake TiroPort/ThreadSummaryClient)는 로컬 Docker Desktop이 다른 프로젝트 컨테이너 15개 이상으로 과부하 상태(여유 메모리 ~64MB까지 확인)라 Testcontainers Postgres 기동이 여러 차례 수 분간 멈추고 로컬 bootRun도 두 차례 원인불명으로 종료됨(코드 예외 없음, 순수 자원 고갈로 판단) — GitHub Actions CI(run 28835136892, `.github/workflows/build.yml`)에서 M0~M8 전체 `./gradlew build`가 1m35s로 성공해 M8AcceptanceTest 통과를 최종 확인함.
