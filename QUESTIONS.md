# QUESTIONS

- PENALTY P0: `01-tech-stack.md:10` says `hibernate-vector` uses the Boot BOM version, and `01-tech-stack.md:32` shows no explicit version. Spring Boot 3.3.5 dependency management does not provide a version for `org.hibernate.orm:hibernate-vector`. Current build pins `6.5.3.Final` only because M0 cannot resolve dependencies otherwise. Confirm the exact allowed version policy.
- PENALTY P0: `01-tech-stack.md:8` lists `spring-boot-starter-security`, but the M0 dependency block at `01-tech-stack.md:24-37` omits it. Should M0 add Spring Security now, or follow the M0 dependency block exactly?
- PENALTY P0: `01-tech-stack.md:15` fixes Testcontainers to 1.19.x, but the latest 1.19.x is 1.19.8 and it pulls docker-java 3.3.6, which fails against local Docker Engine 29.1.3. Testcontainers 1.20.6 still fails here. I upgraded Testcontainers to 2.0.5 so Gradle can download docker-java 3.7.1 and run without Docker API property injection. Confirm whether this spec deviation is acceptable.
- PENALTY P0: `02-domain-model.md` includes `Chunk` and `SlackIngestLog` entities/repositories, but `04-database.md` creates their tables only in V2 and V4. Creating those JPA entities during M1 makes Hibernate `ddl-auto: validate` fail. I am deferring them to their migration milestones.
- PENALTY P0: `01-tech-stack.md:41` names `org.springframework.ai:spring-ai-openai-spring-boot-starter`, but Spring AI 1.0.9 publishes `org.springframework.ai:spring-ai-starter-model-openai` as the OpenAI starter. I used the published 1.0.x artifact so dependencies resolve.
- PENALTY P0: Adding the Spring AI OpenAI starter makes OpenAI chat/image/audio/moderation autoconfig require an API key on startup. I disabled Spring AI models by default and require `SPRING_AI_MODEL_EMBEDDING=openai` with `OPENAI_API_KEY` to enable real embeddings. This extra env var is not listed in `01-tech-stack.md`.
- PENALTY P0: `10-milestones.md:M2 Acceptance 7` requires a manual real `OPENAI_API_KEY` semantic-search check after AXR-team coordination. `OPENAI_API_KEY` is not set in this environment, so I verified M2 with the required fake-embedding Testcontainers test and real curl error paths only. Real OpenAI semantic ranking remains unverified.
- PENALTY P0: `05-editing-plane.md` requires Java to call the editing-plane kick endpoint, but `01-tech-stack.md` and `.env.example` do not define a Java-side editing-plane base URL. I added `EDITING_PLANE_URL` with default `http://localhost:8081`; confirm the intended configuration key.
- PENALTY P0: `06-ingest-slack.md` requires replying with a document URL, but `01-tech-stack.md` does not define the public mydoc base URL. I added `MYDOC_BASE_URL` with default `http://localhost:8080`; confirm the intended configuration key.
- PENALTY P0: `06-ingest-slack.md` says M4 includes summary LLM integration, but `01-tech-stack.md` says the Anthropic starter for summary/correction is added at M5. I implemented the M4 summary parser behind `ThreadSummaryClient` and verified it with a fake client; real Anthropic wiring remains for M5 unless the milestone ordering is corrected.
- PENALTY P0: `10-milestones.md:M4 Acceptance 5` requires a manual real Slack workspace test after AXR-team coordination. `SLACK_BOT_TOKEN` and `SLACK_APP_TOKEN` are not set in this environment, so I verified M4 with the required fake Slack/fake LLM Testcontainers test only. Real Slack ingestion remains unverified.
- RESOLVED: `10-milestones.md:13` requires `docker compose up -d && ./gradlew bootRun`. Local port 5432 was held by `sim-db-1`; I temporarily stopped it, ran the M0 curl checks, then restored it.
- RESOLVED: `09-coding-guidelines.md:39` requires at least one `M0: ...` commit. I initialized git in `/Users/boram/mydoc` after M0 verification.
